# DeepSeek API（Phase 3 起使用）
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-v4-pro

# SQLite 数据库路径（相对 backend/ 或绝对路径）
SQLITE_PATH=./data/app.db

# 查询安全限制
MAX_QUERY_ROWS=1000
SQL_TIMEOUT_SECONDS=30

# CORS 允许的前端源（逗号分隔）
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
